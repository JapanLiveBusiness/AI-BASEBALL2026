name: Deploy production

on:
  push:
    branches:
      - main-AI-BASEBALL
  workflow_dispatch:

concurrency:
  group: production-ai-baseball
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
      TS_OAUTH_SECRET: ${{ secrets.TS_OAUTH_SECRET }}
      TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
    steps:
      - name: Validate deploy secrets
        shell: bash
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          missing=0
          for name in DEPLOY_HOST DEPLOY_USER DEPLOY_PORT; do
            if [ -z "${!name}" ]; then
              echo "::error::$name is not available to this workflow"
              missing=1
            fi
          done
          if { [ -n "$TS_OAUTH_CLIENT_ID" ] && [ -z "$TS_OAUTH_SECRET" ]; } || { [ -z "$TS_OAUTH_CLIENT_ID" ] && [ -n "$TS_OAUTH_SECRET" ]; }; then
            echo "::error::TS_OAUTH_CLIENT_ID and TS_OAUTH_SECRET must be configured together"
            missing=1
          fi
          if [ -z "$TS_OAUTH_CLIENT_ID" ] && [ -z "$TAILSCALE_AUTHKEY" ]; then
            echo "::error::Configure TS_OAUTH_CLIENT_ID + TS_OAUTH_SECRET or TAILSCALE_AUTHKEY"
            missing=1
          fi
          exit "$missing"

      - name: Connect to Tailscale with OAuth
        if: ${{ env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '' }}
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ env.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ env.TS_OAUTH_SECRET }}
          tags: tag:ci
          ping: server-01

      - name: Connect to Tailscale with legacy auth key
        if: ${{ env.TS_OAUTH_CLIENT_ID == '' || env.TS_OAUTH_SECRET == '' }}
        uses: tailscale/github-action@v4
        with:
          authkey: ${{ env.TAILSCALE_AUTHKEY }}
          ping: server-01

      - name: Resolve and verify production Tailscale peer
        shell: bash
        env:
          DEPLOY_HOST: ${{ secrets.DEPLOY_HOST }}
          DEPLOY_PORT: ${{ secrets.DEPLOY_PORT }}
        run: |
          set -euo pipefail

          resolved=""
          for attempt in $(seq 1 12); do
            echo "Tailscale peer discovery attempt $attempt/12"
            tailscale status

            resolved="$(tailscale status --json | jq -r '
              [
                .Peer[]
                | select(.HostName == "server-01" and .Online == true)
                | (.TailscaleIPs // [])[0]
              ]
              | map(select(. != null))
              | .[0] // empty
            ')"

            if [ -n "$resolved" ]; then
              break
            fi

            sleep 5
          done

          if [ -z "$resolved" ]; then
            echo "::error::Online Tailscale peer server-01 was not found after waiting for tailnet propagation"
            exit 1
          fi

          if [ "$resolved" != "$DEPLOY_HOST" ]; then
            echo "::warning::DEPLOY_HOST does not match the current server-01 Tailscale address; using the live peer address for this deployment"
          fi

          echo "DEPLOY_HOST_RESOLVED=$resolved" >> "$GITHUB_ENV"
          # DERP relay is a valid Tailscale path. tailscale ping may return non-zero
          # when a direct peer-to-peer path is not established, even after pong.
          tailscale ping -c 3 "$resolved" || echo "::warning::Direct Tailscale path not established; validating SSH over the available relay path"
          nc -vz -w 5 "$resolved" "$DEPLOY_PORT"
          timeout 10 bash -c "</dev/tcp/$resolved/$DEPLOY_PORT"

      - name: Deploy to production server
        shell: bash
        env:
          DEPLOY_USER: ${{ secrets.DEPLOY_USER }}
          DEPLOY_SHA: ${{ github.sha }}
        run: |
          ssh \
            -o BatchMode=yes \
            -o ConnectTimeout=15 \
            -o StrictHostKeyChecking=no \
            -o UserKnownHostsFile=/dev/null \
            "$DEPLOY_USER@$DEPLOY_HOST_RESOLVED" \
            "DEPLOY_SHA='$DEPLOY_SHA' bash -s" <<'REMOTE'
          set -e
          export APP_DIR="/opt/hawks-ai/ai-baseball2026"
          export DATA_DIR="/opt/hawks-ai/data"
          export BRANCH="main-AI-BASEBALL"
          export CONTAINER_NAME="hawks-app"
          export IMAGE_NAME="hawks-app"
          export PORT="8501"

          cd "$APP_DIR"
          git fetch origin "$BRANCH"
          git show "origin/$BRANCH:scripts/deploy-production.sh" > /tmp/ai-baseball-deploy.sh
          chmod +x /tmp/ai-baseball-deploy.sh
          /tmp/ai-baseball-deploy.sh
          REMOTE

      - name: Verify protected public site
        shell: bash
        env:
          SITE_URL: https://ai-baseball.f-polaris.jp/
          CF_ACCESS_CLIENT_ID: ${{ secrets.CF_ACCESS_CLIENT_ID }}
          CF_ACCESS_CLIENT_SECRET: ${{ secrets.CF_ACCESS_CLIENT_SECRET }}
        run: bash scripts/verify-protected-site.sh
