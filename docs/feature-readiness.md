# 機能稼働状況と実装準備

2026-09-04 時点の公開画面とコードを照合した一覧です。画面に表示する状態は `feature_readiness.py` を正とします。

| 優先度 | 機能・導線 | 現在の状態 | 稼働している範囲 | 次の実装 |
|---|---|---|---|---|
| P0 | BET入力 | ベータ | 新規登録、未確定・確定の保存、ハンデ込み損益計算 | 編集、削除、未確定BETの精算、入力処理の共通化 |
| P0 | 収支マップ | ベータ | 確定BETの総収支、勝敗、的中率、ROI、累積収支 | BET入力ページとの二重実装を解消し、精算導線を追加 |
| P1 | 予想結果 | ベータ | 固定予測3件、履歴バックテスト、CSV出力 | 全NPB予測の試合結果を自動照合し、0件表示を解消 |
| P1 | AI詳細 | 試験運用 | 旧分析画面の読み込み、共有データへの保存 | 約8,000行の `app.py` 実行を機能別モジュールへ分割し、外部ハンデ取得を失敗許容化 |
| P2 | 球団別詳細 | 実装予定 | 12球団の一覧表示のみ | 球団カードを選択可能にし、戦績・先発・直近成績へ遷移 |

## 稼働確認済み

- HOMEから各既存ページへの遷移
- 本日の試合一覧とAI PICK表示
- 本日のAI予測、信頼度、最上位予想
- 予想結果の表表示、検索、CSVダウンロード
- 収支マップの並び替え、グラフ操作

## 実装時の共通条件

- BETデータの保存先とスキーマを1か所に集約する。
- JSON更新は一時ファイルからの置換と排他制御を行い、同時操作による欠損を防ぐ。
- 「編集」「削除」「精算」には安定したレコードIDと更新日時を使用する。
- 外部サイト取得が失敗しても画面全体を止めず、最終成功データと取得状態を表示する。
- 操作可能に見える要素には、実処理または明確な「実装予定」表示を持たせる。

## 今回解消した準備上の不具合

- AI詳細から旧 `app.py` を実行すると `__file__` がページ側を指し、保存先が `pages/data/` へ逸れる問題を修正した。
- AI詳細内の試合履歴・BET精算は、ルートの共有 `data/`（本番では `/app/data` ボリューム）を使用する。
